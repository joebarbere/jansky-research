# Results files permitted to carry a synthetic source in packaged papers.
# Add a file ONLY after verifying the corresponding paper's own text states the
# synthetic/offline provenance of the numbers it quotes. Keep a justification per entry.
# (Deliberately empty as of 2026-07-31 — every reviewed paper quotes real-data results.)
# ecallisto_census_metrics.json — the paper is explicitly a synthetic-validated census statistic
# (abstract + §: "validated here on synthetic data with a real sunspot record"); the real
# multi-cycle ingest is documented future work (plans/joss-rnaas-todo, reproduce target comment).
ecallisto_census_metrics.json
# stokesv_discovery_metrics.json — mixed source string ("synthetic recover-a-known + real
# RACS-mid epoch pair"): the paper labels its synthetic fixture in the figure captions and the
# real census derives from the force-tracked results/stokesv_discovery_realtargets.csv.
# Source-field split is queued in the committed-real-results migration.
stokesv_discovery_metrics.json
# The vgpra VALIDATION leg's metrics, committed 2026-08-13 so the four synthetic numbers the
# abstracts quote are checkable evidence rather than macro-file survivals (referee finding).
# Synthetic by design -- it is the recover-a-known, not a real-data result.
vgpra_synthetic_metrics.json
# The ecallisto_pipeline SYNTHETIC-DAY leg, split out 2026-08-23. The paper's twelve macro uses
# all describe the synthetic day and say so ("On a synthetic day (a real burst at N stations
# plus single-station interference)"), so these are the numbers its abstract quotes and they
# have to be checkable. The real archive day keeps ecallisto_metrics.json; before the split both
# legs wrote one file and one macro set, and the real day's values reached the synthetic
# sentences -- the abstract typeset as "a real burst at 0 stations ... confirms exactly 0 event".
ecallisto_synthetic_metrics.json
# The typeii SYNTHETIC leg, split out 2026-08-24 (round-4 referee): the paper's purity and
# SNR-curve numbers had NO committed results file, because preserve_live_results correctly
# refuses to let the offline leg write into the real census file. Same pattern as
# vgpra_synthetic_* / ecallisto_synthetic_*.
typeii_synthetic_metrics.json
